import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class darkGray here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class darkGray extends Actor
{
    /**
     * Act - do whatever the darkGray wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public darkGray(){
        getImage().scale(getImage().getWidth()/10, getImage().getHeight()/10);
    }
    public void act()
    {
        // Add your action code here.
    }
}
